name: Integration Tests

on:
  push:
    tags:
      - "integration-*"

jobs:
  validate_secrets:
    name: Validate API Keys
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/catthehacker/ubuntu:act-latest
    outputs:
      secrets_available: ${{ steps.check.outputs.available }}
    steps:
      - name: Check if secrets are set
        id: check
        run: |
          if [ -n "${{ secrets.ILOVEIMG_PUBLIC_KEY }}" ] && [ -n "${{ secrets.ILOVEIMG_SECRET_KEY }}" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
            echo "✓ API keys are configured"
          else
            echo "available=false" >> $GITHUB_OUTPUT
            echo "✗ API keys are missing"
            echo "Please configure ILOVEIMG_PUBLIC_KEY and ILOVEIMG_SECRET_KEY in repository secrets"
            exit 1
          fi

  integration_test:
    name: Integration Tests
    needs: validate_secrets
    if: needs.validate_secrets.outputs.secrets_available == 'true'
    strategy:
      fail-fast: true
      matrix:
        python-version: ["3.10", "3.14"]
    uses: ./.gitea/workflows/jobs/integration-test.yml
    with:
      python-version: ${{ matrix.python-version }}
    secrets:
      ILOVEIMG_PUBLIC_KEY: ${{ secrets.ILOVEIMG_PUBLIC_KEY }}
      ILOVEIMG_SECRET_KEY: ${{ secrets.ILOVEIMG_SECRET_KEY }}
